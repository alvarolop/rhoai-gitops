# Topology Aware Scheduling disabled - uncomment below if needed to prevent SchedulingGated deadlocks
# for GPU serving workloads. Requires topologyAwareScheduling: true in values.yaml and topology
# configuration in ResourceFlavor specs.
# See: https://kueue.sigs.k8s.io/docs/concepts/topology_aware_scheduling/
#
# {{- if and .Values.distributedWorkloads.enabled .Values.distributedWorkloads.topologyAwareScheduling }}
# ---
# apiVersion: kueue.x-k8s.io/v1beta1
# kind: Topology
# metadata:
#   name: rhoai-hostname
#   annotations:
#     argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
#     argocd.argoproj.io/sync-wave: "9"
# spec:
#   levels:
#     - nodeLabel: kubernetes.io/hostname
# {{- end }}
